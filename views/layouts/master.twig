<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{{ page.title ?? store.name }}</title>
    <meta name="description" content="{{ page.description ?? store.description }}" />
    <meta name="keywords" content="Ø£Ø²ÙŠØ§Ø¡, ÙØ³Ø§ØªÙŠÙ†, Ù…ÙˆØ¶Ø©, ØªØ³ÙˆÙ‚, Ù…ØªØ¬Ø± Ù†Ø³Ø§Ø¦ÙŠ, {{ store.name }}, Ø¹Ø±ÙˆØ¶ Ø§Ù„Ø£Ø²ÙŠØ§Ø¡" />
    <link rel="canonical" href="{{ app.request.uri }}" />
    <meta property="og:title" content="{{ page.title ?? store.name }}">
    <meta property="og:description" content="{{ page.description ?? store.description }}">
    <meta property="og:image" content="{{ page.image ?? store.logo }}">
    <meta property="og:url" content="{{ app.request.uri }}">
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary_large_image">
    <script>
        document.addEventListener("DOMContentLoaded", function() {
            document.querySelectorAll("img:not([alt])").forEach(img => {
                img.alt = "{{ store.name }} - ØµÙˆØ±Ø© Ø§Ù„Ù…Ù†ØªØ¬";
            });
        });
    </script>
    <script type="application/ld+json">
    {
      "@context": "https:
      "@type": "Store",
      "name": "{{ store.name }}",
      "url": "{{ app.request.uri }}",
      "description": "{{ store.description }}",
      "image": "{{ store.logo }}",
      "address": {
        "@type": "PostalAddress",
        "addressCountry": "SA"
      }
    }
    </script>
    <link rel="stylesheet" href="{{ asset('styles/main.css') }}">
</head>
<body>
<header>
    <div class="header-top"></div>
    <div class="container header">
        <button class="icon-btn mobile-toggle">â˜°</button>
        <a href="/" class="brand">{{ store.name }}</a>
        <nav class="nav-desktop">
            {% for link in store.menu %}
                <a href="{{ link.url }}">{{ link.title }}</a>
            {% endfor %}
        </nav>
        <div class="icons">
            <a href="/search" class="icon-btn">ğŸ”</a>
            <a href="/wishlist" class="icon-btn">â™¡</a>
            <a href="/cart" class="icon-btn">ğŸ›’</a>
            <a href="/customer" class="icon-btn">ğŸ‘¤</a>
        </div>
    </div>
</header>
<div class="mobile-nav">
    <button id="close-mobile-nav" class="close-btn">âœ•</button>
    <nav style="margin-top:25px;">
        {% for link in store.menu %}
            <a href="{{ link.url }}" style="display:block; margin-bottom:14px; font-size:17px;">
                {{ link.title }}
            </a>
        {% endfor %}
    </nav>
</div>
<main>
    {% block content %}{% endblock %}
</main>
<footer class="footer">
    <div class="brand">{{ store.name }}</div>
    <div class="links">
        <a href="/page/about">Ù…Ù† Ù†Ø­Ù†</a>
        <a href="/page/privacy">Ø³ÙŠØ§Ø³Ø© Ø§Ù„Ø®ØµÙˆØµÙŠØ©</a>
        <a href="/page/terms">Ø§Ù„Ø´Ø±ÙˆØ· ÙˆØ§Ù„Ø£Ø­ÙƒØ§Ù…</a>
        <a href="/contact">ØªÙˆØ§ØµÙ„ Ù…Ø¹Ù†Ø§</a>
    </div>
    <div class="socials">
        {% if store.social.instagram %}
            <a href="{{ store.social.instagram }}">ğŸ“¸</a>
        {% endif %}
        {% if store.social.twitter %}
            <a href="{{ store.social.twitter }}">ğŸ¦</a>
        {% endif %}
        {% if store.social.snapchat %}
            <a href="{{ store.social.snapchat }}">ğŸ‘»</a>
        {% endif %}
    </div>
    <div class="copy">
        Ø¬Ù…ÙŠØ¹ Ø§Ù„Ø­Ù‚ÙˆÙ‚ Ù…Ø­ÙÙˆØ¸Ø© Â© {{ "now"|date("Y") }} â€” {{ store.name }}
    </div>
</footer>
<button id="backToTop" class="back-to-top">â†‘</button>
<script src="{{ asset('js/main.js') }}"></script>
</body>
</html>
